//          loop - 14


// var arr = userInput[0].split(" ");
var arr = [1, 2, 3, 4, 5];
let hcf = 0;

for (let i = 1; i <= arr.length; i++) {
    if (arr % i === 0);
    hcf = i;
    console.log(hcf);
}
let lcm = arr / hcf
console.log(lcm);
// console.log(arr);
